<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         failOnWarning="true"
         failOnDeprecation="true"
         failOnNotice="true"
>
    <php>
        <ini name="error_reporting" value="-1" />
        <!--
            Both <server> and <env> are needed: docker-compose.yml sets
            APP_ENV=dev as a real container-wide environment variable (not
            scoped to the web server), which PHP's CLI SAPI auto-populates
            into $_ENV before this file is even read. Symfony's
            KernelTestCase::createKernel() checks $_ENV['APP_ENV'] before
            $_SERVER['APP_ENV'], so a <server>-only override is silently
            ignored inside that container — WebTestCase then boots the
            kernel as "dev", which never got framework.test: true
            (config/packages/framework.yaml's when@test block), and
            createClient() fails. <env force="true"> is the one that
            actually overrides $_ENV.
        -->
        <env name="APP_ENV" value="test" force="true" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
        <server name="SYMFONY_DEPRECATIONS_HELPER" value="disabled" />
        <server name="KERNEL_CLASS" value="App\Kernel" />
    </php>

    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <testsuite name="functional">
            <directory>tests/Functional</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
