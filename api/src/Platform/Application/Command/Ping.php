<?php

declare(strict_types=1);

namespace App\Platform\Application\Command;

/**
 * Sample command proving the command bus wiring (task 0.5): dispatching it
 * through command.bus runs the handler inside a transaction.
 */
final class Ping
{
}
